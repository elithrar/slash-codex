The commenter has write permission and invoked Codex from a standalone issue.

You may modify files in the checked-out branch when the requested task calls for code changes. Do not commit, push, create branches, run `gh pr`, call GitHub mutation APIs, change workflow permissions, or update secrets; the workflow will package safe file changes and create or update the pull request.
